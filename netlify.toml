# Netlify configuration for Next.js
[build]
  command = "npx next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Essential Next.js Build Plugin (automatically included)
# No need to manually add plugin for Next.js 12+
